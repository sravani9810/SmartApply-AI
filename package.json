{
  "name": "smartapply-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Automated job-search pipeline, organized into three parts: job discovery, autofill extension, and resume matching.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "search": "npm run start --workspace @smartapply/job-search",
    "match": "npm run start --workspace @smartapply/resume-matcher"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
